# Security policy

Please report vulnerabilities privately through GitHub Security Advisories for the aq-ui repository. Do not open a public issue containing an exploit or sensitive details.

Only the latest prerelease and latest stable release receive security fixes. The editor packages render content but do not execute code. Raw Markdown HTML is disabled by default and the opt-in HTML path remains sanitized; reports showing a sanitizer or URL-protocol bypass are treated as high priority.
