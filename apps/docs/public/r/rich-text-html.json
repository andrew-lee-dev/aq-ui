{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "rich-text-html",
  "type": "registry:lib",
  "title": "Rich Text Html",
  "description": "Open-code aq-ui shared library for Rich Text Html.",
  "author": "aq-ui contributors",
  "version": "0.1.0-alpha.0",
  "dependencies": [
    "@tiptap/core@^3.30.1",
    "@tiptap/html@^3.30.1",
    "happy-dom@^20.8.9"
  ],
  "registryDependencies": [],
  "files": [
    {
      "path": "lib/rich-text-html.ts",
      "type": "registry:lib",
      "content": "import type { Extensions, JSONContent } from \"@tiptap/core\"\nimport { generateHTML, generateJSON } from \"@tiptap/html\"\n\ninterface RichTextHTMLConversionOptions {\n  /**\n   * Use the same schema extensions that produced the document. Keeping the\n   * list explicit prevents a server conversion from silently dropping custom\n   * nodes or marks.\n   */\n  extensions: Extensions\n}\n\n/** Convert canonical Tiptap JSON to HTML without creating an editor instance. */\nfunction generateRichTextHTML(\n  value: JSONContent,\n  { extensions }: RichTextHTMLConversionOptions\n) {\n  return generateHTML(value, extensions)\n}\n\n/** Parse HTML into canonical Tiptap JSON without creating an editor instance. */\nfunction parseRichTextHTML(\n  value: string,\n  { extensions }: RichTextHTMLConversionOptions\n) {\n  return generateJSON(value, extensions) as JSONContent\n}\n\nexport { generateRichTextHTML, parseRichTextHTML }\nexport type { RichTextHTMLConversionOptions }\n"
    }
  ],
  "meta": {
    "integrity": "sha256-2f596cf51e3f6b09d353689302c39c440e450a73ffb7be19fb5fe09a6826cca5",
    "api": [
      {
        "name": "generateRichTextHTML",
        "kind": "function",
        "line": 14,
        "signature": "function generateRichTextHTML(value: JSONContent, { extensions }: RichTextHTMLConversionOptions)",
        "description": "Convert canonical Tiptap JSON to HTML without creating an editor instance.",
        "parameters": [
          {
            "name": "value",
            "kind": "parameter",
            "type": "JSONContent"
          },
          {
            "name": "options",
            "kind": "parameter",
            "type": "RichTextHTMLConversionOptions"
          }
        ],
        "usage": "generateRichTextHTML(/* arguments */)"
      },
      {
        "name": "parseRichTextHTML",
        "kind": "function",
        "line": 22,
        "signature": "function parseRichTextHTML(value: string, { extensions }: RichTextHTMLConversionOptions)",
        "description": "Parse HTML into canonical Tiptap JSON without creating an editor instance.",
        "parameters": [
          {
            "name": "value",
            "kind": "parameter",
            "type": "string"
          },
          {
            "name": "options",
            "kind": "parameter",
            "type": "RichTextHTMLConversionOptions"
          }
        ],
        "usage": "parseRichTextHTML(/* arguments */)"
      },
      {
        "name": "RichTextHTMLConversionOptions",
        "kind": "interface",
        "line": 4,
        "signature": "interface RichTextHTMLConversionOptions",
        "members": [
          {
            "name": "extensions",
            "kind": "property",
            "type": "Extensions",
            "description": "Use the same schema extensions that produced the document. Keeping the list explicit prevents a server conversion from silently dropping custom nodes or marks."
          }
        ]
      }
    ],
    "usage": {
      "importPath": "@/lib/rich-text-html",
      "importStatement": "import { generateRichTextHTML } from \"@/lib/rich-text-html\"",
      "primaryExport": "generateRichTextHTML",
      "example": "generateRichTextHTML(/* arguments */)"
    },
    "ssr": true
  }
}
